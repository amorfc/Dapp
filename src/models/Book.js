class Book {
    constructor(title, author, image_url, category, isbn, last_renting_date, borrower_schoolNo, is_borrowed) {
        this.title = title
        this.author = author
        this.image_url = image_url
        this.category = category
        this.isbn = isbn
        this.last_renting_date = last_renting_date
        this.borrower_schoolNo = borrower_schoolNo
        this.is_borrowed = is_borrowed
    }
}